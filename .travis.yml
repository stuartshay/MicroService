language: csharp
dist: xenial
env:
  global:
    - Configuration=Release
    - ASPNETCORE_ENVIRONMENT=Staging
dotnet: 3.1.401
mono: none
branches:
  only:
  - master
  - /^release\/.*$/
  - develop
before_install:
  - chmod +x build.sh
script:
  - dotnet --info
  - ./build.sh -target=sonar
